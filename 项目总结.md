# PlantSystem 项目总结

## 项目概述

一套基于 STM32F103 的植物环境监测与远程控制智能系统，包含嵌入式固件、OneNET 云平台、网页前端三部分。

- **主控**: STM32F103C8 (Cortex-M3, 72MHz)
- **通信**: ESP8266 WiFi 模块 (USART2, 115200bps)
- **云平台**: OneNET MQTT (mqtts.heclouds.com:1883)
- **前端**: 单页 HTML5 (绿色气泡卡片 UI，移动端优先)

---

## 硬件引脚分配

| 组件 | 引脚 | 说明 |
|------|------|------|
| ESP8266 | USART2 (PA2-TX, PA3-RX) | WiFi AT 指令通信，最终进入透传模式 |
| DHT11 | PB9 | 数字温湿度传感器，单总线协议 |
| 土壤湿度 | PA6 (ADC1_CH6) | 模拟量 → 百分比 (0-100%) |
| 光敏传感器 | PA1 (ADC1_CH1) | 模拟量 → lux 光照强度 |
| LCD TFT | PA5(SCL),PA7(SDA),PA4(CS),PB0(RST),PB1(RS),PB10(BLK) | 128x160 SPI ST7735S |
| 风扇 | PC14 | 继电器高电平触发 |
| 水泵 | PC15 | 继电器高电平触发 |
| 补光灯 | PA8 | 推挽输出 |
| 按键 S1-S4 | PB15,PB14,PB13,PB12 | 上拉输入，按下为低 |

---

## 目录结构

```
PlantSystem/
├── Users/
│   └── main.c                           # 主入口 + 主循环
├── Drivers/
│   ├── BSP/
│   │   ├── PlantSystem/                 # 核心业务逻辑 (状态管理/页面/控制)
│   │   ├── dht11/                       # DHT11 温湿度传感器驱动
│   │   ├── esp8266/                     # ESP8266 WiFi AT 指令 + 透传
│   │   ├── onenet/                      # MQTT 封包/解包/连接/订阅
│   │   ├── lcd/                         # TFT LCD 显示驱动 + 中文字库
│   │   ├── key/                         # 按键扫描 (消抖 + 边沿检测)
│   │   ├── adc/                         # ADC1 初始化 + 单次采样
│   │   ├── ldr/                         # 光敏电阻 → 光照强度 lux
│   │   ├── ts/                          # 土壤湿度 ADC → 百分比
│   │   ├── fan/                         # 风扇 GPIO 宏定义
│   │   ├── bump/                        # 水泵 GPIO 宏定义
│   │   └── led/                         # 补光灯 GPIO 控制
│   ├── STM32F1xx_HAL_Driver/            # ST 官方 HAL 库
│   └── CMSIS/                           # ARM Cortex-M3 核心支持
├── PlantSystem_web/                     # 网页前端 (独立 git 仓库)
│   ├── index.html                       # SPA 页面 (直连 OneNET API)
│   ├── server.js                        # Node.js 本地代理 (备用)
│   ├── config.local.json                # OneNET 鉴权参数
│   └── image/                           # 图标 SVG/PNG 素材
├── Output/                              # 编译产物 (.o, .hex, .axf)
└── Projects/MDK-ARM/                    # Keil MDK 工程文件
```

---

## 数据流与通信链路

### 上行 (传感器 → 云端)

```
DHT11/ADC/GPIO 读取
  → PlantSystem_UpdateSensors()      # 每 2 秒读 DHT11，每循环读 ADC
  → MQTT_PublishPlantData()          # 每 3 秒拼装 JSON 并发布
  → onenet.mqtt_publish_data()       # 构建 MQTT PUBLISH 二进制帧
  → esp8266.esp8266_send_data()      # USART2 透传发送
  → ESP8266 → WiFi → OneNET
  → Topic: $sys/8AV8W3KkCb/plant/thing/property/post
```

### 下行 (云端 → 设备)

```
网页/OneNET 下发控制指令
  → OneNET → MQTT 推送到设备
  → Topic: $sys/8AV8W3KkCb/plant/thing/property/set
  → ESP8266 UART 中断接收 → g_uart_rx_buf[512]
  → MQTT_ProcessIncoming()           # 主循环中轮询
  → esp8266_wait_receive()           # 两次查询计数稳定 = 接收完成
  → onenet.mqtt_receive_handle()     # 解析 MQTT 帧 topic + payload
  → PlantSystem_HandleCloudPayload() # 解析 JSON 字段执行控制
  → mqtt_send_response()             # 回执到 set_reply topic
```

---

## 主循环流程 (main.c)

```
1. 系统初始化: HAL → 时钟 72MHz → 延时库 → USART1 115200
2. 外设初始化: LCD / 土壤湿度 / 光敏 / 按键 / 风扇 / LED / 水泵
3. ESP8266 初始化: 退出透传 → AT → 复位 → STA 模式 → 连 WiFi → 获取 IP → 连云服务器 → 进入透传
4. MQTT 初始化: 构建登录凭据 → CONNECT → SUBSCRIBE (订阅 set topic)
5. while(1):
   ├── PlantSystem_UpdateSensors()    # 传感器读数
   ├── uart_printf()                  # 串口打印全部状态
   ├── MQTT_ProcessIncoming()         # 检查并处理云端下发
   ├── 按键处理 → 切换 LCD 页面
   ├── LCD 刷新当前页面
   ├── MQTT_PublishPlantData()        # 上报 8 个属性 (id 递增)
   ├── MQTT_ProcessIncoming()         # 上报后立即检查回执
   ├── MQTT_PollForMs(3000)           # 分段 3 秒延时 (每 50ms 检查一次 MQTT)
   ├── mqtt_send_heart()              # MQTT PINGREQ 保活
   └── MQTT_ProcessIncoming()         # 心跳后检查
```

## 上报的属性

| 属性名 | 数据来源 | 说明 |
|--------|---------|------|
| temperature | DHT11 data[2]+data[3] | 温度 (℃) |
| air_humidity | DHT11 data[0]+data[1] | 空气湿度 (%) |
| soil_humidity | ADC_CH6 → TS_GetData() | 土壤湿度 (%) |
| lighting_intensity | ADC_CH1 → LDR_LuxData() | 光照强度 (lux) |
| auto_mode | remote_control_mode | 自动/远程模式 |
| fan | PC14 GPIO | 风扇状态 |
| bump | PC15 GPIO | 水泵状态 |
| LED | PA8 GPIO | 补光灯状态 |

---

## LCD 三个页面

| 页面 | 按键 | 功能描述 |
|------|------|---------|
| Page 1 | S1 | 环境数据页: 温度、空气湿度、土壤湿度、光照、设备开/关状态 |
| Page 2 | S1 | 本地控制页: S2 切换手动/自动，S3 选择设备(风扇/水泵/补光灯)，S4 开关 |
| Page 3 | S1 | 阈值设置页: S2 选择参数(温度/湿度/光照)，S3/S4 增减阈值 |

---

## 自动/远程模式

- **核心变量**: `remote_control_mode` (0=自动, 1=远程)
- **进入远程**: 网页下发 fan/bump/LED 任一控制 → `remote_control_mode = 1`
- **自动控制暂停**: `Fan_Bump_LED_status()` 中若 `remote_control_mode == 1` 则直接返回
- **恢复自动**: 网页下发 `auto_mode=true` → `PlantSystem_HandleCloudPayload()` 中检测到 → `remote_control_mode = 0`
- **上报**: 每轮 publish 都上报 `auto_mode` 状态

### 自动控制规则

| 条件 | 动作 | 默认阈值 |
|------|------|---------|
| 温度 > 阈值 | 开风扇 | 25°C |
| 土壤湿度 < 阈值 | 开水泵 | 10% |
| 光照 < 阈值 | 开补光灯 | 200 lux |

---

## ES8266 通信特点

- 缓冲区: **512 字节** (比较有限)
- 接收判定: 中断累加计数 `esp8266_cnt`，主循环两次查询 `esp8266_cnt == esp8266_cntPre` 认为接收完成
- 初始化: 8 步骤 (退出透传 → AT 测试 → 软复位 → STA 模式 → 单连接 → 连 WiFi → TCP 连接 → 进入透传)

---

## MQTT 通信特点

- **手动二进制组帧**: CONNECT/PUBLISH/SUBSCRIBE/PINGREQ/DISCONNECT 全部在代码中逐字节构建
- **Token 鉴权**: version=2018-10-31&res=...&et=...&method=sha1&sign=...
- **端到端延迟**: 网页→OneNET→ESP8266→STM32→回执链路长，偶发超时

---

## 网页前端 (index.html)

- **通信方式**: 浏览器直连 OneNET API (Web Crypto API 生成 token)
- **UI**: 气泡卡片布局，绿色渐变背景，移动端适配 (max-width 420px)
- **数据刷新**: 每 5 秒自动轮询 + R 键手动刷新
- **在线判断**: 根据属性 `time` 时间戳 ± 15 秒在线 / 60 秒延迟 / 超时离线
- **控制指令**: POST set-device-property，等待 800ms 后重新读取状态确认
- **配置持久化**: localStorage 存储产品 ID/设备名/密钥
- **备用代理**: server.js (Node.js，当前因网络环境直连替代)

---

## 已知问题与限制

1. 512 字节 UART 接收缓冲区较小，高负载可能丢包
2. 接收判定基于两次计数相等而非超时，不够稳健
3. 控制为"真实状态显示"模式而非"乐观更新"，开关不会立刻变状态
4. DHT11 单总线协议存在偶发读取失败 (有超时保护)
5. JSON 解析是简单字符串查找而非完整 JSON 解析器，容错有限
6. OneNET 会保留设备最后的属性值，设备离线时网页仍可能显示旧数据